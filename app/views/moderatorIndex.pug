extends layout

block scripts
    script(src='/static/bundle.js')

block navbar
    nav
        ul.list-unstyled
            li
                a(href='/moderator/create_user') Registrer bruker
            li
                a(href='/moderator/activate_user') Aktiver bruker
            li
                a(href='/moderator/change_card') Mistet kort
            li
                a(href='/moderator/deactivate_users') Deaktiver brukere

block content
    .center(ng-repeat='alert in alerts', ng-class='{"fade": alert.fade}')
        uib-alert(ng-bind='alert.message', type='{{alert.type}}', close='alert.close()')
    .container(ng-view='')
